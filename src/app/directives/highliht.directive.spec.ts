import { HighlihtDirective } from './highliht.directive';

describe('HighlihtDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlihtDirective();
    expect(directive).toBeTruthy();
  });
});
